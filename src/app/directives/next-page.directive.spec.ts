import { NextPageDirective } from './next-page.directive';

describe('NextPageDirective', () => {
  it('should create an instance', () => {
    const directive = new NextPageDirective();
    expect(directive).toBeTruthy();
  });
});
